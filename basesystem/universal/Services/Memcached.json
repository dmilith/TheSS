{
    "softwareName": "Memcached",
    "domain": "localhost",
    "install": {
        "commands": "sofin get memcached",
        "expectOutput": "All done"
    },
    "start": {
        "commands": "
printf 'Launching Memcached\n' >> SERVICE_PREFIX/service.log 2>&1
SERVICE_ROOT/exports/memcached -s SERVICE_PREFIX/service.sock -l SERVICE_ADDRESS -p $(cat SERVICE_PREFIX/.ports/0) -P SERVICE_PREFIX/service.pid -d
"
    }
}