
/* This is default definition used as base for all igniters */

{
    "formatVersion": "0.16",

/* defines name of entry in ~/SoftwareData/ */
    "name": "No-Name",

    "domain": "",

/* defines pool of ports used by service */
    "portsPool": 1,

/* defines disk space check. By default user has to have at least 1 GiB free disk space */
    "minimumRequiredDiskSpace": 1,

/* defines name of software available in Sofin definitions */
    "softwareName": "No-Name",

    "dependencyOf": "",

/* reqad only access to git repository for web applications */
    "repository": "",

/* by default assign dynamic port for service   */
    "staticPort": -1,

    /* alwaysOn with true will always invoke babysitter to watch for service pid validity */
    "alwaysOn": true,

    /* watchPort with true will always watch for service port with babysitter */
    "watchPort": true,

    /* autoStart with true will cause service to start automatically */
    "autoStart": true,

    "reportAllErrors": true,
    "reportAllInfos": false,
    "reportAllDebugs": false,

/* a list of igniters required to launch this software */
    "dependencies": [],

/* default schedulerActions format:             */
/*   "schedulerActions": [                      */
/*     {                                        */
/*         "cronEntry": "0 0/10 * * * ?",       */
/*         "commands": "my shell commands" */
/*     },                                       */
/*     …                                        */
/* ],                                           */
    "schedulerActions": [
    ],
    "install": {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 0
    },
    "configure":  {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "start":  {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "afterStart":  {
        "commands": "
touch SERVICE_PREFIX/.running",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "stop":  {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "afterStop":  {
        "commands": "
test -f SERVICE_PREFIX/service.pid && svddw $(cat SERVICE_PREFIX/service.pid) && rm SERVICE_PREFIX/service.pid
test -f SERVICE_PREFIX/.running && rm SERVICE_PREFIX/.running",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "reload":  {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "validate": {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    },
    "babySitter": {
        "commands": "",
        "expectOutput": "",
        "expectOutputTimeout": 60
    }
}
